var startNum = parseInt(prompt("Where should I start counting?"))
var endNum = parseInt(prompt("Where should I stop counting?"))
while (startNum <= endNum) {
    console.log(startNum);
    startNum++;
}