// The password
var password = "blueturtle"

// Make variable for what the user puts in
var typed_password = prompt("What is the password?")

// Alerts
if (password == typed_password){
  alert("Good job yo!")
} else {
  alert("WRONG!!! Try again!")
}

console.log("Password is" + password)
console.log("typed_password is" + typed_password)
