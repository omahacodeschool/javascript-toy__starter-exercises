var secretNumber = 13;

var params = prompt("What's the secret number?");

if (params == secretNumber) {
  alert("That's correct!");
} else {
  alert("Sorry, the secret number is actually " + secretNumber);
};